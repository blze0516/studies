# Test Strategy Review Skill

Review proposed tests by risk. Identify the smallest set that covers critical domain rules, boundaries, failures, persistence integrity, concurrency, API contract, and critical user paths.
